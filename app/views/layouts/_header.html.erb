

<% if !user_signed_in? %>

  <%= render 'static_pages/header' %>
  
<% elsif !request.path.gsub(/\d/, "number").match("/tests/number/questions").nil? %>

  <%= render 'questions/header' %>

<% elsif !request.path.gsub(/\d/, "number").match("/tests/number/responses").nil? %>

  <%= render 'responses/header' %>

<% else %>  


<% end %>


